APP_NAME="Sistem Panen Sawit"
APP_ENV=production
APP_DEBUG=false
APP_KEY=base64:xqOUX4Af6s9MCwoYIfs/yifOGT8f9vZ6Hr2o4iXH5mQ=
APP_URL=https://web-production-b42f8.up.railway.app
APP_TIMEZONE=Asia/Jakarta
# Set this if behind proxy/CDN
TRUSTED_PROXIES=*

LOG_CHANNEL=stderr
LOG_LEVEL=info

DB_CONNECTION=pgsql
# PENTING: Isi variabel berikut SATU PER SATU di Railway (Variables tab) gunakan value dari Connection string Postgres.
# JANGAN menaruh banyak pasangan key=value dalam satu variable (itu penyebab error sebelumnya).
DB_HOST=
DB_PORT=5432
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=
DB_CHARSET=utf8
# Mode SSL: require (aman), prefer (default), disable (tidak disarankan di production)
PGSSLMODE=require
# Schema Postgres (biarkan public bila tidak perlu custom)
PG_SCHEMA=public
# Opsi tambahan (misal: application_name=panen,search_path=public) pisahkan dengan spasi atau biarkan kosong
DB_PG_OPTIONS=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_SECURE_COOKIE=true
SANCTUM_STATEFUL_DOMAINS=web-production-93d7e.up.railway.app

MEMCACHED_HOST=127.0.0.1

# Redis (optional – jika nanti pakai Redis di Railway, ganti host & password)
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

# --- Keamanan Tambahan (opsional) ---
# FORCE_HTTPS=true akan dipakai jika kamu set logic di middleware / AppServiceProvider
FORCE_HTTPS=true

# Pastikan variabel di atas cocok dengan konfigurasi service Postgres Railway sebelum deploy.
