APP_NAME="Sistem Panen Sawit"
APP_ENV=production
APP_DEBUG=false
APP_KEY=
APP_URL=https://web-production-1a02e.up.railway.app

LOG_CHANNEL=stderr
LOG_LEVEL=info

DB_CONNECTION=pgsql
# Penting: JANGAN set DATABASE_URL di file .env contoh ini. Railway akan meng-inject DATABASE_URL sendiri via Variables.
# Jika Anda set di sini dengan placeholder, koneksi DB akan gagal (host tidak bisa di-resolve).
# DATABASE_URL=
PGSSLMODE=prefer

# --- LOCAL DEBUG / DEVELOPMENT ---
# Internal private domain TIDAK akan resolve dari laptop kamu. Untuk koneksi lokal pakai public/proxy host dari tab Connect (Public Network).
# Format untuk Laravel (.env lokal):
# DATABASE_URL=pgsql://postgres:PFXmmrFdlJiIbSKgjHOAOXaAkLPTLVar@mainline.proxy.rlwy.net:24717/railway?sslmode=prefer
# Pastikan port (contoh 24717) sama dengan yang muncul di dashboard kamu.
# Jika password punya karakter spesial (spasi, @, #, /, ?), URL-encode karakter itu.

# Contoh perintah psql lokal:
# PGPASSWORD=PFXmmrFdlJiIbSKgjHOAOXaAkLPTLVar psql -h mainline.proxy.rlwy.net -p 24717 -U postgres -d railway -v sslmode=prefer

# Contoh JDBC (DataGrip / IntelliJ):
# jdbc:postgresql://mainline.proxy.rlwy.net:24717/railway
# User: postgres
# Password: PFXmmrFdlJiIbSKgjHOAOXaAkLPTLVar
# (JANGAN pakai skema "pgsql://" untuk JDBC; harus diawali "jdbc:postgresql://")

# Jika lebih suka pecah variabel (JANGAN gabung dengan DATABASE_URL, pilih salah satu) hapus DATABASE_URL lalu isi yang di bawah:
# DB_HOST=<RAILWAY_PRIVATE_DOMAIN>
# DB_PORT=5432
# DB_DATABASE=railway
# DB_USERNAME=postgres
# DB_PASSWORD=PFXmmrFdlJiIbSKgjHOAOXaAkLPTLVar
# DB_CHARSET=utf8
# PG_SCHEMA=public

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_SECURE_COOKIE=true

MAIL_MAILER=log

# Seeder user domain & optional passwords
USER_EMAIL_DOMAIN=sahabatagro.co.id
# (Opsional) Override default seeder passwords; jika tidak diisi pakai default di seeder
# ADMIN_PASSWORD=
# MANAGER_PASSWORD=
# OPERATOR_PASSWORD=
